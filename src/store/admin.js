// import Api from "@/utils/api";

const state = {
  selected_user: {
    fullname: "Adebisi Borokini",
    fname: "First Name",
    lname: "Last Name"
  }
};

const getters = {};

const mutations = {};

const actions = {};

export default {
  state,
  getters,
  mutations,
  actions
};
